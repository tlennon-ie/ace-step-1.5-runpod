# FastAPI and server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ACE-Step (installed from git or local)
# acestep @ git+https://github.com/ace-step/ACE-Step-1.5.git

# Core dependencies (these are also required by acestep)
# Note: torch/torchaudio versions are pinned in Dockerfile for CUDA compatibility
torch==2.10.0
torchaudio==2.10.0
transformers==4.57.0
accelerate==1.12.0
safetensors==0.5.2
einops==0.8.2
scipy==1.14.1
numpy==2.0.2

# Audio processing
soundfile==0.13.1
librosa==0.10.2

# LLM backend
vllm==0.15.0

# Utilities
pydantic==2.12.5
huggingface-hub==0.34.0
tqdm==4.67.1
